import React from "react";
import { WrappedActionBuilderProps } from "application/components/noteBuilder";

// ZettelId generates the ID automatically — no user interaction during note creation.
export function ZettelIdWrapper(_props: WrappedActionBuilderProps) {
    return <></>;
}
